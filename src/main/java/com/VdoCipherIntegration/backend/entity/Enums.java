package com.VdoCipherIntegration.backend.entity;

public class Enums {
    public enum Gender {
        MALE, FEMALE
    }

    public enum VideoStatus {
        PROCESSING,READY
    }

    public enum AccountStatus {
        WAITING_VERIFICATION, ACTIVE, SUSPENDED
    }

    public enum DeviceOS {
        ANDROID, IOS
    }
}
